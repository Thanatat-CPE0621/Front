import { mount } from '@vue/test-utils'

describe('login', () => {
  test('login page', () => {
    const n = null;
    expect(n).toBeNull();

  })

  test('login page2222', () => {
    const n = null;

    expect(n).toBeDefined();
  })
})

describe('dashboard', () => {
  test('onpage', () => {
    const n = null;
    expect(n).toBeNull();
  })

  test('move page', () => {
    const n = null;
    expect(n).toBeNull();
  })
})
